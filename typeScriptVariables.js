"use strict";
let age = 25;
console.log(age);
let username = "Test name";
console.log(username);
username = "Test name 2"; // reassigning new value to the variable username
console.log(username);
let isActive = true;
console.log(isActive);
let text = "Hello World"; // can store any type of value in the data type any.
console.log(text);
text = 100; //Data type of the variable text is any, so we can assign a number to it.
console.log(text);
let randomValue = "Hello World Second"; // can store any type of value in the data type unknown. This is safer than any because we need to check the type of the variable before using it.
console.log(randomValue);
